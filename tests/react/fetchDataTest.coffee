React = require 'react'
fetchData = require '../../src/lib/react/fetchData'
shiv = require '../../src'

describe 'fetchData', ->
  it 'should run the fetchData static method on the route handlers passed', ->

